# For-delta-SHGC-paper-review

This repository provides the code, input files, dataset preparation workflow, and model training procedures used in our study. It is intended to support a transparent and reproducible review of the data generation and surrogate-model development process.

The repository includes the following materials:

1. **IDF files based on the DOE Midrise Apartment model**  
   The IDF files were developed based on the DOE Midrise Apartment reference building model. In these files, all conventional glazing systems were replaced with smart glazing. The dynamic response of the smart windows was implemented through the EnergyPlus **Energy Management System (EMS)**. Specifically, the windows were switched to the high-temperature state when the `Zone Predicted Sensible Load to Cooling Setpoint Heat Transfer Rate` was greater than 0, and switched to the low-temperature state when the `Zone Predicted Sensible Load to Heating Setpoint Heat Transfer Rate` was greater than 0.

2. **`EPW files/`**  
   This folder contains the weather data required for dataset generation in `.epw` format. The weather dataset includes 60 representative cities from each of Climate Zones 1 to 7, as well as 33 cities from Climate Zone 8.
   Due to the restriction of Github,please use this link to get all EPW files:
   https://zenodo.org/records/21276685?token=eyJhbGciOiJIUzUxMiJ9.eyJpZCI6ImIyNWQ2Njc4LTFiNmMtNDYyZC04YzliLWY2OTY4NGM5NmUwZiIsImRhdGEiOnt9LCJyYW5kb20iOiIxZGQwODhkYzVjZTcwMzZjZjllOWM4NzI4MDUzNjZlMSJ9.yTkDmCFa_iI530PKcNBvSr6-Ig7f0SUuIcdQ0t3usCYsRqWlIVjWCXz072Jbc0MJGvFmPX3NzIl4yxhotzBfGw

4. **`from Tsol and E to SHGC.py`**  
   This script uses the `pywincalc` library, developed by Lawrence Berkeley National Laboratory, to enable rapid calculation from optical properties to engineering design parameters.

5. **`tsol+E+U+SHGC.xlsx`**  
   This file contains the calculation results generated by `from Tsol and E to SHGC.py`.

6. **`dataset preparation.py`**  
   This script prepares the simulation dataset using the `model.idf` file and a large set of weather files. Latin hypercube sampling is adopted to generate different glazing design cases, and EnergyPlus simulations are then used to obtain the corresponding cooling and heating energy consumption of the building model.

7. **`datasets from EP.csv`**  
   This file is the dataset generated by `dataset preparation.py`.

8. **`surrogate model training.py`**  
   This script trains machine-learning surrogate models using XGBoost. The model inputs include annual weather features and glazing engineering design parameters, while the outputs are building cooling and heating energy consumption.The dataset is evaluated using a 10-fold cross-validation scheme. In each fold, the cities are stratified by latitude, with 90% of the cities used for training and 10% for testing to rigorously evaluate the model's spatial generalizability across unseen geographical locations.

9. **`ml results/`**  
   This folder contains the generated surrogate models in `.pkl` format.
